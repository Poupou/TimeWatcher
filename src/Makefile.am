bin_PROGRAMS = timeWatcher

timeWatcher_LDADD = $(OPENCV_LIBS)
timeWatcher_CPPFLAGS = $(OPENCV_CFLAGS)
timeWatcher_SOURCES = main.cc

#timeWatcher_LDFLAGS = `pkg-config --libs opencv`
#timeWatcher_CXXFLAGS = `pkg-config --cflags opencv`

